# Вопросы по теме "Jenkins"

## Знание

1. Какой порт используется для связи между Jenkins контроллером и агентами?
   - [x] 50000
   - [ ] 8080
   - [ ] 8443
   - [ ] 22

2. На каком языке программирования пишется Jenkinsfile?
   - [x] Groovy
   - [ ] Python
   - [ ] JavaScript
   - [ ] Java

3. Какое ключевое слово используется для определения переменных в Groovy?
   - [x] def
   - [ ] var
   - [ ] let
   - [ ] const

4. Какая встроенная переменная содержит инкрементный номер текущей сборки в Jenkins?
   - [x] env.BUILD_NUMBER
   - [ ] env.BUILD_ID
   - [ ] env.JOB_NUMBER
   - [ ] env.PIPELINE_ID

5. Какая команда используется для выполнения shell-команды на Linux агенте в Jenkins?
   - [x] sh 'command'
   - [ ] exec 'command'
   - [ ] bash 'command'
   - [ ] run 'command'

6. Какой блок в декларативном синтаксисе определяет действия после завершения конвейера в Jenkins?
   - [x] post
   - [ ] finally
   - [ ] after
   - [ ] cleanup

7. Какой протокол обычно используется для взаимодействия с постоянными агентами в Unix-подобных системах?
   - [x] SSH
   - [ ] HTTP
   - [ ] JNLP
   - [ ] FTP

8. Какой тип параметра используется для выбора из предопределенного списка значений в Jenkins?
   - [x] choice
   - [ ] select
   - [ ] dropdown
   - [ ] list

9. Что означает `agent any` в декларативном синтаксисе Jenkins?
   - [x] Конвейер может выполняться на любом доступном агенте
   - [ ] Конвейер не будет выполняться ни на одном агенте
   - [ ] Конвейер выполнится только на мастер-узле контроллера
   - [ ] Конвейер требует специально настроенный именованный агент

10. Почему не рекомендуется запускать сборки на узле-контроллере Jenkins?
    - [x] Сборки могут получить доступ к конфигурации контроллера
    - [ ] Контроллер физически не может выполнять задачи сборки
    - [ ] Это требует приобретения дополнительных коммерческих лицензий
    - [ ] Контроллер поддерживает только запуск агентов, но не сборки

11. Какой блок в секции `post` выполняется независимо от результата сборки в Jenkins?
    - [x] always
    - [ ] success
    - [ ] failure
    - [ ] unstable

12. Сервис Jenkins, который управляет агентами и распределяет задачи, называется:
    - [x] Jenkins controller
    - [ ] Jenkins agent
    - [ ] Jenkins server
    - [ ] Jenkins node manager

13. Сервис Jenkins, который выполняет задачи сборки, называется:
    - [x] Jenkins agent
    - [ ] Jenkins controller
    - [ ] Jenkins pipeline executor
    - [ ] Jenkins build manager

14. В каком файле обычно хранятся определения конвейеров Jenkins?
    - [x] Jenkinsfile
    - [ ] pipeline.yml
    - [ ] build.groovy
    - [ ] pipeline.jenkins

15. Какая команда используется для выполнения команд Windows на агенте Jenkins?
    - [x] bat 'command'
    - [ ] cmd 'command'
    - [ ] win 'command'
    - [ ] exec 'command'

16. Какой блок определяет этапы выполнения в декларативном pipeline Jenkins?
    - [x] stages
    - [ ] steps
    - [ ] phases
    - [ ] blocks

17. Что делает директива `when` в декларативном pipeline Jenkins?
    - [x] Определяет условия для выполнения стадии
    - [ ] Устанавливает таймаут для выполнения стадии
    - [ ] Указывает на какие сборки ссылаться
    - [ ] Определяет время запуска сборки

18. Какая секция используется для определения переменных окружения в декларативном pipeline?
    - [x] environment
    - [ ] env
    - [ ] variables
    - [ ] params

19. Что означает параллельное выполнение стадий в Jenkins pipeline?
    - [x] Одновременное выполнение нескольких независимых стадий
    - [ ] Запуск нескольких сборок одновременно для разных веток
    - [ ] Использование нескольких агентов для одной стадии
    - [ ] Ускорение работы за счет добавления CPU ядер

20. Какой тип credentials НЕ поддерживается Jenkins по умолчанию?
    - [x] Биометрические данные (отпечатки пальцев)
    - [ ] Username with password
    - [ ] SSH Username with private key
    - [ ] Secret text

## Понимание

1. Какой образ Docker официально рекомендуется для запуска Jenkins?
   - jenkins/jenkins:lts

2. Через какой объект осуществляется доступ к переменным окружения в Jenkins pipeline?
   - env

3. Какая встроенная переменная содержит имя задачи (job) в Jenkins?
   - JOB_NAME

4. Как называется секция в декларативном конвейере, которая выполняется после завершения всех этапов?
   - post

5. Какая встроенная переменная содержит путь к рабочей директории текущей сборки?
   - WORKSPACE

6. Какая команда в Groovy используется для вывода текста в консоль Jenkins?
   - echo

7. Как называется встроенная переменная, содержащая номер текущей сборки?
   - BUILD_NUMBER

8. Какой метод используется для выполнения shell-команд на Linux агенте?
   - sh

9. Какой тип параметра позволяет пользователю выбрать значение из выпадающего списка?
   - choice

10. Какая встроенная переменная содержит полный URL текущей сборки?
    - BUILD_URL

## Интеграция

1. Напишите Jenkinsfile для простого конвейера, который выполняет следующие шаги:
   - Клонирует репозиторий GitHub
   - Работает на любом доступном агенте
   - имеет этапы: `Сборка (Build)` и `Тестирование (Test)`
   - В блоке `Сборка` выполните команду `maven clean install`
   - В блоке `Тестирование` выполните команду `maven test`
   - В блоке post добавьте действия для always, success и failure
     - always: вывести сообщение "Работа конвейера завершена."
     - success: вывести сообщение "Все этапы прошли успешно!"
     - failure: вывести сообщение "Произошла ошибка в конвейере."
2. Напишите Jenkinsfile для простого конвейера, который выполняет следующие шаги:
   - Клонирует репозиторий GitHub
   - Работает на агенте с меткой "linux"
   - имеет этапы: `Сборка (Build)` и `Тестирование (Test)`
   - В блоке `Сборка` выполните команду `composer install`
   - В блоке `Тестирование` выполните команду `composer test`
   - В блоке post добавьте действия для always, success и failure
     - always: вывести сообщение "Работа конвейера завершена."
     - success: вывести сообщение "Все этапы прошли успешно!"
     - failure: вывести сообщение "Произошла ошибка в конвейере."
3. Напишите Jenkinsfile для простого конвейера, который выполняет следующие шаги:
   - Клонирует репозиторий GitHub
   - Работает на агенте с меткой "windows"
   - имеет этапы: `Сборка (Build)` и `Тестирование (Test)`
   - В блоке `Сборка` выполните команду `msbuild /t:Build`
   - В блоке `Тестирование` выполните команду `vstest.console.exe MyTests.dll`
   - В блоке post добавьте действия для always, success и failure
     - always: вывести сообщение "Работа конвейера завершена."
     - success: вывести сообщение "Все этапы прошли успешно!"
     - failure: вывести сообщение "Произошла ошибка в конвейере."
4. Напишите Jenkinsfile для простого конвейера, который выполняет следующие шаги:
   - Клонирует репозиторий GitHub
   - Работает на агенте с меткой "builder"
   - имеет этапы: `Сборка (Build)` и `Тестирование (Test)`
   - В блоке `Сборка` выполните команду `docker build -t my-app .`
   - В блоке `Тестирование` выполните команду `docker run --rm my-app ./run-tests.sh`
   - В блоке post добавьте действия для always, success и failure
     - always: вывести сообщение "Работа конвейера завершена."
     - success: вывести сообщение "Все этапы прошли успешно!"
     - failure: вывести сообщение "Произошла ошибка в конвейере."
5. Напишите Jenkinsfile для простого конвейера, который выполняет следующие шаги:
   - Клонирует репозиторий GitHub
   - Работает на агенте с меткой "nodejs"
   - имеет этапы: `Сборка (Build)` и `Тестирование (Test)`
   - В блоке `Сборка` выполните команду `npm install`
   - В блоке `Тестирование` выполните команду `npm test`
   - В блоке post добавьте действия для always, success и failure
     - always: вывести сообщение "Работа конвейера завершена."
     - success: вывести сообщение "Все этапы прошли успешно!"
     - failure: вывести сообщение "Произошла ошибка в конвейере."
6. Напишите Jenkinsfile для простого конвейера, который выполняет следующие шаги:
   - Клонирует репозиторий GitHub
   - Работает на агенте с меткой "python"
   - имеет этапы: `Сборка (Build)` и `Тестирование (Test)`
   - В блоке `Сборка` выполните команду `pip install -r requirements.txt`
   - В блоке `Тестирование` выполните команду `pytest`
   - В блоке post добавьте действия для always, success и failure
     - always: вывести сообщение "Работа конвейера завершена."
     - success: вывести сообщение "Все этапы прошли успешно!"
     - failure: вывести сообщение "Произошла ошибка в конвейере."
